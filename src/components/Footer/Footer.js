import React, { Component } from 'react'
import './footer.scss';

export default class Navbar extends Component {
    render() {
        return (
            <footer>
                
            </footer>
        );
    }
}